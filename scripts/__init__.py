"""
Make the `scripts` directory an importable package.

This file is intentionally left empty; it ensures `python -m scripts.run_comprehensive_subject`
and `import scripts` work when `PYTHONPATH` or the working directory points to the project root.
"""
