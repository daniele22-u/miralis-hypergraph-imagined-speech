# Checkpoint Sessione — 15 Marzo 2026

## Cosa è stato fatto

### 1. Setup pipeline SSH su WSL (Windows Subsystem for Linux)

Identificata la configurazione della macchina SSH: non Linux puro, ma **WSL** su Windows.
- H5 files accessibili al path WSL: `/mnt/c/Users/students/Desktop/Daniele/data/`
- Formato file: `SS_SS.h5` (es. `00_01.h5`, `00_02.h5`, ...) — soggetto × sessione
- Conda env `daniele_311` già installato in `/home/daniele_u/miniconda3/envs/daniele_311/`
- VS Code con Remote SSH già funzionante

Dopo il pull da main, la repo è risultata già completa di tutto il necessario:
- `scripts/build_tensors.py` — pipeline standalone EEG_01 + EEG_02
- `scripts/setup_data_symlinks.sh` — crea symlink da path Windows a `data/`
- `configs/label_schemes/` — tutti i JSON delle label (word-based) committati e git-tracked

### 2. Come usare la pipeline sul WSL

**Step 1 — Setup dati (una tantum):**
```bash
git pull origin main
bash scripts/setup_data_symlinks.sh /mnt/c/Users/students/Desktop/Daniele/data
```

**Step 2 — Crea tensori dagli H5:**
```bash
conda activate daniele_311
python scripts/build_tensors.py \
  --h5_dir /mnt/c/Users/students/Desktop/Daniele/data \
  --mode aggregated --resume
```

**Step 3 — Eseguire i notebook:**
Non serve `nohup jupyter nbconvert` per uso interattivo. Le opzioni disponibili:
- **VS Code** (più comodo): apri il `.ipynb` con Remote SSH + estensione Jupyter → seleziona kernel `daniele_311` → esegui celle
- **JupyterLab nel browser**: `jupyter lab --no-browser --port=8889` sul WSL + `ssh -L 8889:localhost:8889 ...` dal Mac → `http://localhost:8889`
- **Headless background** (solo per run lunghi/disconnessione): `nohup jupyter nbconvert ... > log.txt 2>&1 &`

**TensorBoard via port forwarding:**
```bash
# Sul WSL:
tensorboard --logdir runs/ --port 6006 --bind_all
# Sul Mac:
ssh -L 6006:localhost:6006 daniele_u@<ip>
# Browser: http://localhost:6006
```

### 3. Aggiornamento EEG_05 — Forward Test + Cluster Sweep

Aggiunte 4 nuove celle a `notebooks/EEG_05_braindecode_raw_baseline_indipendant.ipynb`:

#### Toggle aggiornato
- `USE_CLUSTERS = True` e `CLUSTER_SCHEME = "concr4"` come default
- Commento aggiornato con tutti gli schemi disponibili (inclusi nuovi: `concr4`, `phon4`, `ward6`)
- Separazione esplicita tra schemi word-based (affidabili) e EEG-based (ARI≈0, solo analisi)

#### Forward Test (cella `keb9jr1klj`)
- Esegue solo **EEGNet** per **5 epoche** sullo schema del TOGGLE
- Scopo: verifica che la pipeline dati, TensorBoard e checkpointing funzionino
- Stampa le istruzioni per avviare TensorBoard e il port forwarding SSH
- Parametro `N_FWD_EPOCHS` configurabile

#### Cluster Sweep (cella `skf891mlre`)
Loop su 5 schemi × 6 modelli con le seguenti feature:

| Parametro | Valore |
|-----------|--------|
| `SWEEP_SCHEMES` | `["phon4", "concr4", "sem5", "pos4", "ward5"]` |
| `SWEEP_MODELS` | tutti e 6 (EEGNet, Shallow, Deep4, Conformer, ATCNet, Labram) |
| `SWEEP_RESUME` | `True` — salta combinazioni già addestrate |
| TensorBoard | `runs/sweep_si/{schema}/{modello}/` |
| Checkpoint | `models/sweep_si_{schema}/{modello}_si.pth` |
| Salvataggio intermedio | `data/interim/sweep_si_results.csv` dopo ogni schema |

Motivazione scelta schemi:

| Schema | k | Motivazione | Imbalance |
|--------|---|-------------|-----------|
| `phon4` | 4 | Luogo articolazione → cortex motoria differenziata (Cooney 2020) | 1.8x |
| `concr4` | 4 | CONCR/AZIONE/STATO/ASTRATTO — substrati neurali distinti (Binder 2011) | 2.4x |
| `sem5` | 5 | 5 aree cerebrali: limbico, motorio, temporale, prefrontale, Broca/Wernicke | 4.8x |
| `pos4` | 4 | Baseline grammaticale: VERBI/SOSTANTIVI/MODIFICATORI/GRAMMATICALI | 1.5x |
| `ward5` | 5 | Baseline data-driven (Ward su SBERT k=5) | 3.3x |

Esclusi: `ward6` (imbalance 34x), `ward4` (6.1x).

#### Tabella comparativa finale (cella `6nm6fwu5gwu`)
- Pivot table: schema (righe) × modello (colonne)
- Heatmap `test_acc` + bar chart `mean_acc vs chance level`
- Top-10 combinazioni per `test_acc`
- Delta rispetto al chance level per schema
- Figura salvata in `figures/sweep_si_results.png`

---

## Aggiornamento 16 Marzo 2026 — Primi risultati su WSL

### Ambiente confermato

La macchina SSH è **WSL su Windows** con:
- CUDA disponibile (`device: cuda`, torch 2.10.0+cu128)
- H5 files al path: `/mnt/c/Users/students/Desktop/Daniele/data/` (formato `SS_SS.h5`)
- Conda env `daniele_311` con braindecode 1.3.2

### Analisi data leakage

Verificato che **non c'è data leakage** in EEG_05:
- `_compute_stats` campiona 500 record da `self.records` che sono esclusivamente record del training set
- Split soggetti disgiunto: `SUBJ_TRAIN=[0-49]`, `SUBJ_VAL=[50-59]`, `SUBJ_TEST=[60-73]`
- Val e test usano `ds_tr.mean, ds_tr.std` passati esplicitamente dal train
- Label schemes derivano da proprietà delle parole, nessun segnale EEG coinvolto

### Risultato baseline 110 classi (run incidentale)

Prima run sul WSL eseguita accidentalmente con `USE_CLUSTERS = False` (raw110, 110 classi) — probabilmente perché i file `labelid2cluster_concr4.json` non erano ancora disponibili in `data/interim/` sulla macchina (gitignored). I risultati sono nei cell output del notebook ma **non erano un test pianificato**:
```
EEGNet           test_acc=0.011  (chance=0.009)
ShallowFBCSPNet  test_acc=0.009
Deep4Net         test_acc=0.009
EEGConformer     test_acc=0.009
ATCNet           test_acc=0.010
Labram           test_acc=0.009
```
Tutti al chance level — coerente con le aspettative. Da ripetere intenzionalmente in futuro solo se si vuole un confronto formale raw110 vs clustering.

**Fix applicato**: `configs/label_schemes/` ora contiene tutti i JSON word-based committati e git-tracked. Dopo `git pull`, `load_label_scheme("concr4", ...)` trova il file anche senza eseguire EEG_00.

### Risultati in corso — concr4 (4 classi, chance=25%)

Run attiva: `eeg05_si_4` con `CLUSTER_SCHEME = "concr4"` (CONCR/AZIONE/STATO/ASTRATTO).

Risultati TensorBoard (Balanced_Accuracy/val, in corso al 16-03-26):

| Modello | val_bacc | Epoche | Tempo |
|---------|----------|--------|-------|
| EEGNet | 0.2491 | 15 | ~1h |
| ShallowFBCSPNet | 0.2542 | 19 | ~1.25h |
| Deep4Net | 0.2503 | 41 | ~2.7h |
| EEGConformer | 0.25 | 7 | ~30 min |
| ATCNet | in corso | — | — |
| Labram | in corso | — | — |

**Interpretazione**: tutti i modelli convergono al chance level (0.25). Le curve TensorBoard sono piatte dall'inizio — i modelli non trovano pattern cross-subject generalizzabili. EEGConformer si ferma dopo 7 epoche senza nessun miglioramento.

Questo è il **baseline atteso** e conferma:
1. La variabilità inter-soggetto (ε²=0.85) domina il segnale
2. Senza domain adaptation, nessun modello generalizza cross-subject
3. Questo risultato è il punto di partenza da cui misurare ogni miglioramento futuro

---

## Prossimo step

1. **[IMMEDIATO]** Attendere completamento ATCNet e Labram per `concr4`
2. **[IMMEDIATO]** Ripetere con `phon4` e `sem5` per completare il quadro baseline
3. **[BREVE]** Aggiungere **Instance Normalization** (Bomatter et al. 2024) in EEG_05 — 1 riga di codice nel Dataset, potenzialmente sufficiente a superare il chance level cross-subject
4. **[BREVE]** Se Instance Norm funziona: sweep completo (5 schemi × 6 modelli) con normalizzazione attiva
5. **[MEDIO]** Graph Attention Networks (GAT) su grafo di connettività EEG
6. **[MEDIO]** Hypergraph Neural Networks DHSLP/DHSLF (obiettivo principale tesi)
