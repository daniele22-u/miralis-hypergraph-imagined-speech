# Checkpoint Sessione — 26 Aprile 2026

## Cosa è stato fatto

### 1. EEG_07e — Cella Confronto Connettività Inter-Soggetti

Aggiunta cella statica (id `197633c8`) in `EEG_07e_build_graphs_tensors.ipynb` per confrontare
la connettività EEG tra soggetti diversi. Salvato come PNG (non widget).

**Confronto implementato (4 rappresentazioni):**
- PCC matrix (heatmap)
- Adj threshold — tutte le coppie con PCC > p50 (NON top-k — questo era il bug vecchio)
- k-NN raw — top-6 per nodo, nessun threshold
- Consensus cache-free — ≥2/5 metriche, chiamate dirette senza cache (evita collisione chiave per stesso word stem tra soggetti diversi)
- HE co-membership — ipergrafo k-NN
- Bar grado per canale

**Bug risolti durante la sessione:**
- `N_CH = 59` hardcoded → `IndexError` su CSV con 61 canali → fix: `N = x_np.shape[0]` dinamico
- `raw == pruned` identici → causa: `knn_edge_index(threshold=p50)` azzera < p50 poi prende top-k, ma i top-k vicini hanno sempre PCC >> p50 → fix: adj_threshold su TUTTE le coppie, non top-k
- `records` vuoto → figura vuota senza errore → fix: `RuntimeError` esplicito + diagnostico
- `COMPARE_SUBJECTS = [68]` non trovava soggetto → fix: `_resolve_subjects()` supporta sia `int` (indice) che `str` (pattern nome es. `"P068"`)

**Commits:**
```
f5d789b feat(eeg07e): aggiungi cella statica confronto connettivita inter-soggetti
2b3d29c fix(eeg07e): N_CH dinamico per soggetto (59 o 61 canali)
e0f3c1b fix(eeg07e): confronto connettivita inter-soggetti con metriche distinte
ffe4442 fix(eeg07e): diagnostico soggetti + supporto nome stringa in COMPARE_SUBJECTS
```

---

### 2. Risultati Analisi Connettività (6 soggetti campione)

Trial: `accendere_img.csv`, k=6, thr=0.157 (p50 PCC globale)

| Soggetto | Archi thr | Archi k-NN | Archi consensus | Gruppo |
|----------|-----------|------------|----------------|--------|
| P002_S005 | **1507** | 245 | 194 | Alta connettività |
| P008_S004 | **1525** | 262 | 237 | Alta connettività |
| P011_S003 | 1285 | 241 | 202 | Alta connettività |
| P000_S001 | 942 | 263 | 208 | Media |
| P014_S001 | 738 | 257 | 185 | Bassa connettività |
| P005_S004 | 679 | 260 | 194 | Bassa connettività |

**Insight critici:**
- **Adj threshold** = metrica più discriminativa: spread 679–1525 (~2x), nessun k-NN constraint
- **Consensus** = stabile 185–237 → NON utile per discriminare soggetti (il filtro multi-metrica comprime le differenze)
- **k-NN** = ~263 fisso per costruzione → non discrimina
- Due gruppi naturali visibili da trial singolo: alta/bassa connettività
- HE co-membership mostra differenze visive drammatiche (P002/P008 = blocchi brillanti, P005/P014 = quasi buio)

---

### 3. Direzione Francesco: Subject Clustering EEG-First

**Approccio precedente chiuso**: usare accuracy come proxy per capacità IS è metodologicamente debole — la varianza inter-soggetto delle accuracies (21–27% in concr4) è troppo bassa.

**Nuova direzione**: clustering soggetti da EEG features → post-hoc accuracy validation.

Notebook dedicato: `EEG_08_subject_clustering.ipynb` (già esistente, da popolare).

---

## Prossimo Step (IMMEDIATO)

**EEG_08_subject_clustering.ipynb**

Feature per-soggetto da calcolare (su campione multi-trial):
1. `thresh_density` — fraz. coppie PCC > p50 (il segnale più forte trovato)
2. `mean_pcc` — media off-diagonale PCC
3. `pcc_spectral_gap` — gap spettrale Laplaciano (misura struttura a blocchi)
4. Band powers (delta/theta/alpha/beta/gamma) mediati sui canali
5. `he_density` — co-membership ipergrafo normalizzata

Pipeline:
- Feature matrix 70 soggetti × N features
- K-Means k=2,3 + Ward gerarchico
- PCA/UMAP visualizzazione
- Post-hoc: overlay accuracy EEG_06

---

## Note Tecniche

- **Bug cache `_cache_key()`**: usa `Path(csv_path).stem` — stessa chiave per stesso word stem tra soggetti diversi. Workaround in cella confronto: chiamate dirette a `pcc_matrix()`, `plv_matrix()` ecc. senza passare per cache. Bug da risolvere alla radice in EEG_07e prima di rieseguire build completo dataset.
- **CSV_ROOT** non è sul filesystem locale (disco esterno/NAS) — i test di connettività richiedono esecuzione nel JupyterLab con dati montati
- Branch: `main` (merged, nessun worktree pendente)
