# Checkpoint — 11 Aprile 2026

## Stato Attuale

### EEG_07 — 5-Fold CV Subject-Independent (4/5 fold completati)
- **4 fold su 5 completati** — 1 fold rimanente
- **Risultati preliminari**: tutti i modelli Braindecode a chance level (~25% bacc su 4 classi ward4)
- Nessun modello si distingue significativamente dagli altri
- Conferma che il problema di imagined speech subject-independent è estremamente difficile con architetture standard
- Risultati finali attesi al completamento del 5° fold

| Modello | test_bacc (media 4 fold, prel.) | Note |
|---------|-------------------------------|------|
| EEGNet | ~25% | chance |
| ShallowFBCSPNet | ~25-26% | lieve varianza |
| Deep4Net | ~25% | chance |
| EEGConformer | ~25% | chance |
| ATCNet | ~25% | chance |
| Labram | ~25% | chance |

### EEG_08 — GCN Avviato (in osservazione)
- Notebook avviato per le prime run di osservazione
- **Architettura aggiornata** rispetto alla versione precedente (vedi sezione dedicata sotto)
- Risultati non ancora disponibili

---

## Aggiornamento Architettura EEG_08

### Motivazione
L'architettura originale di EEG_08 usava `GCNConv` standard e un grafo k-NN basato su distanze geodetiche — scelte arbitrarie senza base in letteratura. L'architettura è stata sostituita con un modello ancorato a un paper reale.

### Paper di Riferimento
> **Lun X, Jia Z, Hou Y, et al.**
> *GCNs-Net: A Graph Convolutional Neural Network Approach for Decoding Time-Resolved EEG Motor Imagery Signals*
> IEEE Transactions on Neural Systems and Rehabilitation Engineering, **2022**, vol. 30, pp. 2447–2455.
> arXiv: [2006.08924](https://arxiv.org/abs/2006.08924)

**Perché questo paper**: è il lavoro GCN più citato per decoding EEG su grafo di elettrodi, con architettura chiara e riproducibile. Task simile (classificazione da EEG multi-canale), stessa struttura di input (canali × tempo).

### Modifiche Introdotte

| Componente | Prima (arbitrario) | Dopo (GCNs-Net) |
|------------|--------------------|-----------------|
| **Grafo** | k-NN da distanze geodetiche | **PCC** (Pearson Correlation Coefficient) tra canali — connettività funzionale |
| **Convoluzione** | `GCNConv` | **`ChebConv` (K=2)** — filtri di Chebyshev, come nell'originale |
| **Toggle** | `K_NEIGHBORS=6` | `PCC_THRESHOLD=0.5`, `CHEB_K=2` |
| **Nomi modelli** | `TemporalGCN_*` | `ChebGCN_*` |

**Adattamento rispetto all'originale**: GCNs-Net usa processing *time-resolved* (ogni timestep = campione separato). Qui manteniamo il **Temporal Encoder** 1D CNN per-nodo, più adatto per classificare trial interi a 384 campioni. Il grafo PCC e ChebConv(K=2) sono invariati.

### Varianti Implementate
- `ChebGCN_2L`: 2 layer ChebConv
- `ChebGCN_3L`: 3 layer ChebConv
- `ChebGCNSkip`: 2 layer con skip connection residuale

---

## Prossimi Passi

### Immediato
1. **Attendere completamento EEG_07** (ultimo fold) → tabella finale mean ± std
2. **Osservare EEG_08** → verificare se ChebGCN supera o raggiunge il baseline Braindecode
   - Domanda chiave: la connettività funzionale (PCC) aggiunge potere espressivo?
   - Se risultati a chance: conferma che il bottleneck è l'alta variabilità inter-soggetto, non l'architettura

### Breve Termine
3. **EEG_09 — GAT** (Graph Attention Network)
   - Sostituire ChebConv con GATConv
   - Visualizzare attention weights sugli elettrodi durante il decoding
   - Paper di riferimento da identificare (es. Veličković et al. 2018 + applicazioni EEG)

4. **EEG_10 — Hypergraph Neural Network** (obiettivo principale tesi)
   - Iperarchi per catturare dipendenze tra gruppi di elettrodi
   - Paper: Li et al. 2025 (DHSLP/DHSLF), Feng et al. 2019 (HGNN)

### Medio Termine (Pathway B)
5. **Instance Normalization** cross-soggetto (Bomatter et al. 2024) — 1 riga per ridurre variabilità
6. **Domain Adaptation** per ridurre gap inter-soggetto
7. **5-fold CV su EEG_08** per confronto statisticamente robusto con EEG_07
