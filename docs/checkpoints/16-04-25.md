# Checkpoint — 16 Aprile 2025

## Cosa è stato fatto

### 1. Pre-calcolo grafi (EEG_07b)
Creato notebook `EEG_07b_precompute_graphs.ipynb` che costruisce dataset PyG completi
`Data(x, edge_index, y, subj)` per tutti i trial (~38.926) e li salva su disco:
- `data/interim/graphs/dataset_pcc_k6.pt` (~7 GB)
- `data/interim/graphs/dataset_plv_k6.pt` (~4 GB)
- `data/interim/graphs/dataset_wpli_k6.pt` (in corso)

Approccio suggerito da Francesco: salvare dataset completo invece dei soli edge_index.
Parallelizzazione con `joblib loky` (spawn, no fork) su 26 core.
Diversi bug risolti lungo la strada: mmap OOM, fork swap overflow, wPLI broadcasting axis.

### 2. Refactoring EEG_08 — dataset da disco
`EEGGraphDataset` aggiornato per caricare da dataset pre-costruito invece di leggere HDF5
ad ogni `__getitem__`. Eliminato bottleneck CPU durante training.
Aggiunti:
- `GRAPH_METHOD` toggle (pcc/plv/wpli/learned/dynamic)
- `PLV_BANDS = [(4,8),(8,13)]` — theta + alpha separati (fix bug docstring)
- `RESULTS_CSV` e `CKPT_BASE` per-metodo (evita falsi resume tra metodi diversi)
- `EEGGraphDatasetLegacy` per metodi learned/dynamic (on-the-fly, no cache)
- Nome run TensorBoard include `GRAPH_METHOD`

### 3. Training completo EEG_08 — tutti i metodi
Eseguiti tutti i modelli ChebGCN su tutti i metodi grafo:

| Metodo | Modello | val_bacc |
|--------|---------|----------|
| PCC | ChebGCN_2L | 0.2527 |
| PCC | ChebGCN_3L | **0.2607** |
| PCC | ChebGCN_Skip | 0.2538 |
| PLV | ChebGCN_2L | 0.2557 |
| PLV | ChebGCN_3L | 0.2522 |
| PLV | ChebGCN_Skip | 0.2503 |
| wPLI | ChebGCN_2L | 0.2511 |
| wPLI | ChebGCN_3L | 0.2528 |
| wPLI | ChebGCN_Skip | 0.2535 |
| Learned | ChebGCN_Learned | 0.2528 |
| Dynamic | ChebGCN_Dynamic | 0.2501 |

Chance level: 25.0%. Tutti i modelli convergono a chance.

### 4. Ablation study
Generato plot `figures/eeg08_ablation_graph_methods.png`:
- Asse X: metodo grafo (PCC, PLV, wPLI, Learned, Dynamic)
- Barre: architettura (2L, 3L, Skip, Learned, Dynamic)
- Linea rossa: chance level 25%

**Conclusione**: nessun metodo di costruzione del grafo supera 26.1% val_bacc.
Il bottleneck non è la topologia del grafo ma la variabilità inter-soggetto (ε²=0.85).

### 5. Aggiornamento EEG_09 e EEG_10
- EEG_09 (GAT + domain adversarial): aggiornato per caricare dataset pre-costruito,
  aggiunto `GRAPH_METHOD` toggle identico a EEG_08, `subj` propagato per adversarial training
- EEG_10 (HGNN): mantenuto on-the-fly (iperedge ≠ edge_index), fix sanity check cell

### 6. Fix vari
- Bug `if bands is None` dentro docstring (PLV/wPLI non funzionavano)
- Bug wPLI broadcasting: `analytic[:,:,np.newaxis]` → `analytic[:,np.newaxis,:]`
- Bug `records` vs `data_list` in `train_gcn` class weights
- Bug falso resume tra metodi diversi (RESULTS_CSV condiviso)
- Filtro `subject_id` non numerico (`ignore_43`)

## Risultati numerici

**Best val_bacc EEG_08**: 26.1% (PCC, ChebGCN_3L) — statisticamente non distinguibile da chance.

## Conclusione sessione

Il fallimento cross-subject con GCN è confermato su 5 metodi di costruzione del grafo
e 5 architetture diverse. Questo è un risultato in sé — supportato teoricamente da
Bomatter et al. 2025 (NeurIPS): servono >200 soggetti per EEG cross-subject.

## Prossimo step

1. Eseguire EEG_09 (GAT + domain adversarial) — stesso ablation su metodi grafo
2. EEG_10 (HGNN) — obiettivo principale tesi, contributo metodologico
3. Valutare subject-specific come controllo (dimostra che il segnale c'è ma è soggetto-dipendente)
